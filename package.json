{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap && npm run build",
    "release": "lerna publish",
    "build": "lerna run build --scope=reffects-*"
  }
}

{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap && npm run build",
    "release": "npm run bootstrap && lerna publish",
    "build": "lerna run build --scope=reffects*",
    "test": "lerna run test --stream"
  }
}
